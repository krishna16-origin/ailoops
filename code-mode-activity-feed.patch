diff --git a/app.py b/app.py
index 58e8909..b762f11 100644
--- a/app.py
+++ b/app.py
@@ -2,6 +2,7 @@ import os
 import os
 import json
 import re
+import difflib
 import asyncio
 from datetime import datetime, timezone
 from typing import List, Dict, Any, Optional
@@ -65,22 +66,31 @@ def get_thinking_config(level: str) -> dict:
 
 
 def get_llm(model_type: str, temperature: float, max_tokens: int) -> ChatNVIDIA:
-    """Create the selected model with the requested completion-token budget."""
+    """Create the selected Chat-mode model (Horus/Osiris/Amun-Ra). Code mode uses
+    its own get_code_llm() with an independent fast/medium/strong tier set."""
     model_name = "nvidia/nemotron-3-nano-omni-30b-a3b-reasoning"
     model_type_clean = (model_type or "balanced").strip().lower()
     if model_type_clean == "fast":
         model_name = "deepseek-ai/deepseek-v4-pro"
     elif model_type_clean == "reasoning":
         model_name = "nvidia/nemotron-3-ultra-550b-a55b"
-    elif model_type_clean in {"gpt120b", "gpt-oss-120b", "openai/gpt-oss-120b"}:
-        model_name = "openai/gpt-oss-120b"
-    elif model_type_clean in {"glm", "glm5.2", "glm-5.2", "kimi", "kimik2.6", "kimi-k2.6"}:
-        # GLM 5.2 is currently end-of-life on the deployed NVIDIA account and
-        # Kimi K2.6 is not provisioned there. Keep old request values compatible
-        # but route them to the available GPT-OSS 120B provider instead.
-        model_name = "openai/gpt-oss-120b"
-    if model_name == "openai/gpt-oss-120b":
-        max_tokens = min(max_tokens, 4096)
+    return ChatNVIDIA(model=model_name, temperature=temperature, max_tokens=max_tokens, timeout=90)
+
+
+CODE_MODEL_MAP = {
+    "fast": "deepseek-ai/deepseek-v4-flash-0731",
+    "medium": "minimaxai/minimax-m3",
+    "strong": "nvidia/nemotron-3-ultra-550b-a55b",
+}
+DEFAULT_CODE_MODEL = "medium"
+
+
+def get_code_llm(model_type: str, temperature: float, max_tokens: int) -> ChatNVIDIA:
+    """Create the selected Code-mode model. Code mode has its own fast/medium/strong
+    tiers, kept separate from Chat mode's Horus/Osiris/Amun-Ra models in get_llm()
+    so the two never collide on the same key."""
+    model_type_clean = (model_type or DEFAULT_CODE_MODEL).strip().lower()
+    model_name = CODE_MODEL_MAP.get(model_type_clean, CODE_MODEL_MAP[DEFAULT_CODE_MODEL])
     return ChatNVIDIA(model=model_name, temperature=temperature, max_tokens=max_tokens, timeout=90)
 
 
@@ -671,6 +681,69 @@ def extract_code_artifact(text: str) -> tuple[str, str, str, dict, dict]:
     return explanation, "", "", files, file_languages
 
 
+def _split_explanation_into_notes(explanation: str) -> List[str]:
+    """Break the model's own explanation into short note bullets — real content
+    from the model, not synthesized narration — for the activity feed's clock-icon
+    lines (e.g. 'Architected CSS styling and engineered component rendering logic.')."""
+    if not explanation:
+        return []
+    # Prefer existing bullet/numbered lines if the model already wrote them.
+    bullet_lines = [ln.strip(" -*\t") for ln in explanation.splitlines() if re.match(r"^\s*([-*]|\d+[.)])\s+\S", ln)]
+    if bullet_lines:
+        return [b for b in bullet_lines if b][:6]
+    # Otherwise split into sentences and keep the short, concrete ones.
+    sentences = re.split(r"(?<=[.!?])\s+", explanation.strip())
+    notes = [s.strip() for s in sentences if 8 <= len(s.strip()) <= 160]
+    return notes[:4]
+
+
+def build_turn_activities(files: dict, code: str, language: str, explanation: str, previous_files: dict) -> tuple[list, dict]:
+    """Build a Claude-Code-style activity trace for one Code-mode turn, using only
+    real signal already available: a genuine per-file diff against what this same
+    session generated last turn (difflib), plus the model's own explanation text
+    split into short notes. No fabricated tool calls are added."""
+    activities: list = []
+    all_files = dict(files) if files else ({"": code} if code else {})
+
+    edited_count = 0
+    viewed_count = 0
+
+    for filename, new_content in all_files.items():
+        display_name = filename or (f"generated.{language}" if language else "generated code")
+        old_content = previous_files.get(filename)
+        if old_content is not None:
+            # This file already existed in the session — genuinely "viewed" it
+            # (it's the context the model was given) before editing it.
+            activities.append({"kind": "view", "text": f"Reviewed the current contents of {display_name} before editing"})
+            viewed_count += 1
+            old_lines = old_content.splitlines()
+            new_lines = new_content.splitlines()
+            sm = difflib.SequenceMatcher(a=old_lines, b=new_lines)
+            additions = deletions = 0
+            for tag, i1, i2, j1, j2 in sm.get_opcodes():
+                if tag in ("replace", "delete"):
+                    deletions += (i2 - i1)
+                if tag in ("replace", "insert"):
+                    additions += (j2 - j1)
+            activities.append({"kind": "edit", "file": display_name, "additions": additions, "deletions": deletions})
+        else:
+            additions = len(new_content.splitlines())
+            activities.append({"kind": "edit", "file": display_name, "additions": additions, "deletions": 0})
+        edited_count += 1
+
+    for note in _split_explanation_into_notes(explanation):
+        activities.append({"kind": "note", "text": note})
+
+    note_count = sum(1 for a in activities if a["kind"] == "note")
+    summary = {
+        "commands": 0,
+        "files_edited": edited_count,
+        "files_viewed": viewed_count,
+        "notes": note_count,
+    }
+    return activities, summary
+
+
 # ---------------------------------------------------------------------------
 # Code mode: direct response generation with HTTP SSE code streaming.
 # Generated code is returned only through the response stream. The frontend
@@ -684,19 +757,18 @@ CODE_GENERATION_TIMEOUT = 90
 
 async def generate_code_once(request: CodeChatRequest, session: dict, progress=None, on_answer_piece=None) -> dict:
     config = get_thinking_config(request.reasoning_level)
-    model_key = (request.model or 'glm').strip().lower()
+    model_key = (request.model or DEFAULT_CODE_MODEL).strip().lower()
+    if model_key not in CODE_MODEL_MAP:
+        model_key = DEFAULT_CODE_MODEL
     await publish_progress(
         progress,
         'code_generation_started',
         'code_generation_started',
         f"Generating code with {config['label']} effort and a {config['max_tokens']}-token budget.",
     )
-    llm = get_llm(model_key, 0.2, config['max_tokens'])
-    code_model_key = model_key
+    llm = get_code_llm(model_key, 0.2, config['max_tokens'])
     thinking_mode = None
-    reasoning_effort = ({'low': 'low', 'medium': 'medium', 'high': 'high'}.get(normalize_thinking_level(request.reasoning_level))
-                        if code_model_key in {'gpt120b', 'gpt-oss-120b', 'openai/gpt-oss-120b', 'kimi', 'kimik2.6', 'kimi-k2.6', 'glm', 'glm5.2', 'glm-5.2'}
-                        else None)
+    reasoning_effort = None
     raw_response = await invoke_model(
         build_code_messages(session['messages'], request.reasoning_level),
         llm,
@@ -714,6 +786,15 @@ async def generate_code_once(request: CodeChatRequest, session: dict, progress=N
         filename: (content[:CODE_MAX_OUTPUT] + '\n… output truncated …' if len(content) > CODE_MAX_OUTPUT else content)
         for filename, content in files.items()
     }
+    previous_files = dict(session.get('code_files') or {})
+    activities, activity_summary = build_turn_activities(files, code, language, explanation, previous_files)
+    # Remember what we just generated so the *next* turn in this session can
+    # diff against real prior content instead of guessing.
+    session_files = session.setdefault('code_files', {})
+    if files:
+        session_files.update(files)
+    elif code:
+        session_files[''] = code
     return {
         'response': explanation,
         'code': code,
@@ -724,6 +805,8 @@ async def generate_code_once(request: CodeChatRequest, session: dict, progress=N
         'thinking_summary': 'Generated directly in the response stream; no file-system actions were run.',
         'thinking_level': config['label'],
         'max_tokens': config['max_tokens'],
+        'activities': activities,
+        'activity_summary': activity_summary,
     }
 
 
diff --git a/frontend/index.html b/frontend/index.html
index 9bab565..24e720d 100644
--- a/frontend/index.html
+++ b/frontend/index.html
@@ -182,36 +182,44 @@
         .message-text pre code { background-color:transparent; padding:0; font-size:0.9em; border:none; }
         .message-text blockquote { border-left:4px solid var(--accent); padding-left:12px; color:var(--text-secondary); margin:12px 0; }
 
-        /* ============== CODE MODE: in-response live diff box ============== */
-        .response-code-diff { margin-top:10px; border:1px solid var(--border-color); border-radius:8px; background-color:var(--bg-main); overflow:hidden; }
-        .response-code-diff[hidden] { display:none; }
-        .response-code-diff-header { display:flex; align-items:center; justify-content:space-between; gap:10px; padding:7px 9px; border-bottom:1px solid var(--border-color); color:var(--text-secondary); font-size:11.5px; }
-        .response-code-diff-status { display:flex; align-items:center; gap:8px; min-width:0; }
-        .response-code-diff-dot { width:7px; height:7px; border-radius:50%; background:var(--accent); flex-shrink:0; animation:inlineStreamPulse 1s ease-in-out infinite; }
-        .response-code-diff-dot.done { animation:none; background:#3fb950; }
-        @keyframes inlineStreamPulse { 0%,100% { opacity:0.3; } 50% { opacity:1; } }
-        .response-code-diff-title { color:var(--text-primary); font-weight:600; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
-        .response-code-diff-count { font:11.5px 'Courier New', Courier, monospace; color:#a9b1bb; white-space:nowrap; }
-        .response-code-diff-tabs { display:flex; gap:3px; padding:5px 8px 0; background-color:var(--bg-main); }
-        .response-code-diff-tab { border:1px solid transparent; border-bottom:0; border-radius:6px 6px 0 0; padding:5px 9px; background:transparent; color:var(--text-secondary); font:11.5px 'Inter',sans-serif; cursor:pointer; }
-        .response-code-diff-tab:hover:not(:disabled) { color:var(--text-primary); background-color:var(--bg-hover); }
-        .response-code-diff-tab.active { color:var(--text-primary); background:var(--bg-input); border-color:var(--border-color); }
-        .response-code-diff-tab:disabled { opacity:0.4; cursor:not-allowed; }
-        .response-diff-box { max-height:240px; overflow:auto; background-color:var(--bg-input); scrollbar-width:thin; scrollbar-color:rgba(255,255,255,0.15) transparent; }
-        .response-diff-box::-webkit-scrollbar { width:4px; height:4px; }
-        .response-diff-box::-webkit-scrollbar-track { background:transparent; }
-        .response-diff-box::-webkit-scrollbar-thumb { background-color:rgba(255,255,255,0.15); border-radius:4px; }
-        .response-diff-box::-webkit-scrollbar-thumb:hover { background-color:rgba(255,255,255,0.25); }
-        .response-diff-empty { padding:10px; color:var(--text-secondary); font:11.5px/1.45 'Courier New', Courier, monospace; }
-        .response-diff-file + .response-diff-file { border-top:1px solid rgba(255,255,255,0.09); }
-        .response-diff-file-header { display:flex; align-items:center; justify-content:space-between; gap:8px; padding:6px 9px; color:var(--text-primary); background-color:var(--bg-hover); border-bottom:1px solid var(--border-color); font:11px 'Courier New', Courier, monospace; }
-        .response-diff-file-name { overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
-        .response-diff-file-stat { color:#aff5b4; white-space:nowrap; }
-        .response-diff-code { margin:0; padding:5px 0; color:var(--text-primary); background-color:var(--bg-input); font:11px/1.45 'Courier New', Courier, monospace; white-space:pre; }
-        .response-diff-line { display:block; min-width:max-content; padding:0 9px; color:#bcebd8; background-color:rgba(16,163,127,.08); }
-        .response-preview-canvas { height:300px; background-color:var(--bg-input); }
-        .response-preview-canvas[hidden] { display:none; }
-        .response-preview-canvas iframe { display:block; width:100%; height:100%; border:0; background:#fff; }
+        /* ============== CODE MODE: workflow trace (steps) ============== */
+        .response-workflow { margin-top:10px; padding-top:2px; }
+        .response-workflow[hidden] { display:none; }
+        .response-workflow-summary { color:var(--text-secondary); font:12px 'Inter',sans-serif; margin:0 0 8px; }
+        .response-workflow-list { display:flex; flex-direction:column; gap:7px; }
+        .response-workflow-row { display:flex; align-items:flex-start; gap:8px; font:13px/1.45 'Inter',sans-serif; color:var(--text-secondary); }
+        .response-workflow-icon { flex:0 0 16px; width:16px; height:16px; margin-top:1px; display:flex; align-items:center; justify-content:center; color:var(--text-secondary); }
+        .response-workflow-icon svg { width:14px; height:14px; }
+        .response-workflow-row.note .response-workflow-icon { color:#8b949e; }
+        .response-workflow-row.edit .response-workflow-icon { color:#79c0ff; }
+        .response-workflow-row.view .response-workflow-icon { color:#8b949e; }
+        .response-workflow-row.working .response-workflow-icon { color:var(--accent); }
+        .response-workflow-note-text { color:var(--text-primary); font-weight:600; }
+        .response-workflow-note-text code { font:12px 'Courier New', Courier, monospace; background:var(--bg-hover); border:1px solid var(--border-color); border-radius:4px; padding:1px 5px; color:var(--text-primary); font-weight:400; }
+        .response-workflow-edit-file { color:var(--text-primary); font:12.5px 'Courier New', Courier, monospace; }
+        .response-workflow-add { color:#3fb950; font:12.5px 'Courier New', Courier, monospace; margin-left:6px; }
+        .response-workflow-del { color:#f85149; font:12.5px 'Courier New', Courier, monospace; margin-left:4px; }
+        .response-workflow-plain { color:var(--text-secondary); }
+        .response-workflow-plain code { font:12px 'Courier New', Courier, monospace; background:var(--bg-hover); border:1px solid var(--border-color); border-radius:4px; padding:1px 5px; color:var(--text-primary); }
+        .code-mode-message .message-prose,
+        .code-mode-message .message-prose p { font-weight:600; color:var(--text-primary); }
+
+        /* ============== CODE MODE: downloadable file cards ============== */
+        .response-file-cards { margin-top:14px; }
+        .response-file-cards[hidden] { display:none; }
+        .response-file-card-list { display:flex; flex-direction:column; gap:8px; }
+        .response-file-card { display:flex; align-items:center; gap:12px; padding:11px 14px; border:1px solid var(--border-color); border-radius:10px; background-color:var(--bg-hover); }
+        .response-file-card-icon { flex:0 0 34px; width:34px; height:34px; border-radius:8px; background-color:var(--bg-main); display:flex; align-items:center; justify-content:center; color:var(--text-secondary); }
+        .response-file-card-icon svg { width:16px; height:16px; }
+        .response-file-card-meta { flex:1 1 auto; min-width:0; }
+        .response-file-card-name { color:var(--text-primary); font:13.5px 'Inter',sans-serif; font-weight:600; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
+        .response-file-card-type { color:var(--text-secondary); font:11.5px 'Inter',sans-serif; margin-top:2px; }
+        .response-file-card-download { flex:0 0 auto; border:1px solid var(--border-color); border-radius:7px; padding:6px 13px; background:var(--bg-main); color:var(--text-primary); font:12.5px 'Inter',sans-serif; font-weight:500; cursor:pointer; }
+        .response-file-card-download:hover { background-color:var(--bg-input); }
+        .response-file-cards-download-all { margin-top:10px; display:inline-flex; align-items:center; gap:7px; border:1px solid var(--border-color); border-radius:7px; padding:7px 14px; background:var(--bg-hover); color:var(--text-primary); font:12.5px 'Inter',sans-serif; font-weight:500; cursor:pointer; }
+        .response-file-cards-download-all:hover { background-color:var(--bg-input); }
+        .response-file-cards-download-all svg { width:14px; height:14px; }
+
 
         .thinking-block { margin-top:2px; }
         .thinking-toggle { background:none; border:none; padding:0; font-size:13px; font-family:inherit; color:var(--text-secondary); cursor:pointer; }
@@ -560,9 +568,9 @@
                 <option value="max">Max</option>
             </select>
             <select id="codeModel">
-                <option value="gpt120b" selected>GPT-OSS 120B</option>
-                <option value="glm">GLM 5.2 (unavailable)</option>
-                <option value="kimik2.6">Kimi K2.6 (unavailable)</option>
+                <option value="fast">Flash</option>
+                <option value="medium" selected>Minimax M3</option>
+                <option value="strong">Nemotron Ultra</option>
             </select>
             <select id="codeReasoningLevel">
                 <option value="low">Low</option>
@@ -579,6 +587,7 @@
     <script src="https://cdn.jsdelivr.net/npm/marked-katex-extension/lib/index.umd.js"></script>
     <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
     <script src="https://cdnjs.cloudflare.com/ajax/libs/dompurify/3.1.5/purify.min.js"></script>
+    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
 
     <script>
         if (typeof marked !== 'undefined' && typeof markedKatex !== 'undefined') {
@@ -712,39 +721,6 @@
             logo.classList.add(`state-${state}`);
         }
 
-        // Maps a language to whatever markup is needed to actually see something
-        // rendered in a sandboxed iframe. Used only for Code Mode "show_preview" results.
-        function buildPreviewDoc(language, code) {
-            const lang = (language || '').trim().toLowerCase();
-            const safeCode = code || '';
-
-            if (lang.includes('html')) {
-                return safeCode;
-            }
-            if (lang.includes('css')) {
-                return `<!DOCTYPE html><html><head><style>${safeCode}</style></head><body style="font-family:sans-serif;padding:20px;"><h1>Heading</h1><p>Sample paragraph text to preview your CSS against.</p><button>Sample Button</button></body></html>`;
-            }
-            if (lang.includes('jsx') || lang.includes('tsx') || lang.includes('react')) {
-                return `<!DOCTYPE html><html><head>
-                    <script src="https://unpkg.com/react@18/umd/react.development.js"><\/script>
-                    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"><\/script>
-                    <script src="https://unpkg.com/@babel/standalone/babel.min.js"><\/script>
-                </head><body><div id="root"></div>
-                <script type="text/babel">
-                    ${safeCode}
-                    try {
-                        const rootEl = document.getElementById('root');
-                        const Comp = typeof App !== 'undefined' ? App : (typeof default1 !== 'undefined' ? default1 : null);
-                        if (Comp) { ReactDOM.createRoot(rootEl).render(React.createElement(Comp)); }
-                    } catch (e) { document.getElementById('root').innerText = 'Preview error: ' + e.message; }
-                <\/script></body></html>`;
-            }
-            // Plain JS or anything else frontend-ish: run inside a minimal page.
-            return `<!DOCTYPE html><html><body>
-                <script>${safeCode}<\/script>
-            </body></html>`;
-        }
-
         function highlightCode(el) {
             el.querySelectorAll('pre code').forEach(codeEl => {
                 hljs.highlightElement(codeEl);
@@ -916,9 +892,9 @@
                 { value: 'reasoning', name: 'Amun-Ra', desc: 'For complex requests' }
             ],
             code: [
-                { value: 'gpt120b', name: 'GPT-OSS 120B', desc: 'Available coding and reasoning model' },
-                { value: 'glm', name: 'GLM 5.2', desc: 'Currently unavailable on the deployed NVIDIA account' },
-                { value: 'kimik2.6', name: 'Kimi K2.6 (Thinking)', desc: 'Currently unavailable on the deployed NVIDIA account' }
+                { value: 'fast', name: 'Flash', desc: 'deepseek-ai/deepseek-v4-flash-0731 — fastest, best for quick edits' },
+                { value: 'medium', name: 'Minimax M3', desc: 'minimaxai/minimax-m3 — balanced speed and quality' },
+                { value: 'strong', name: 'Nemotron Ultra', desc: 'nvidia/nemotron-3-ultra-550b-a55b — strongest, best for hard problems' }
             ]
         };
 
@@ -1183,36 +1159,29 @@
                 body.appendChild(statusDiv);
 
                 if (currentMode === 'code') {
-                    const diffBox = document.createElement('section');
-                    diffBox.className = 'response-code-diff';
-                    diffBox.hidden = true;
-                    diffBox.setAttribute('aria-live', 'polite');
-                    diffBox.innerHTML = `
-                        <div class="response-code-diff-header">
-                            <div class="response-code-diff-status"><span class="response-code-diff-dot"></span><span class="response-code-diff-title">Generating code</span></div>
-                            <span class="response-code-diff-count">0 lines</span>
-                        </div>
-                        <div class="response-code-diff-tabs">
-                            <button type="button" class="response-code-diff-tab active" data-response-view="diff">Diff</button>
-                            <button type="button" class="response-code-diff-tab" data-response-view="preview" disabled>Preview</button>
-                        </div>
-                        <div class="response-diff-box"><div class="response-diff-empty">Waiting for generated code…</div></div>
-                        <div class="response-preview-canvas" hidden><iframe title="Generated code preview" sandbox="allow-scripts"></iframe></div>
+                    contentDiv.classList.add('code-mode-message');
+
+                    const workflow = document.createElement('section');
+                    workflow.className = 'response-workflow';
+                    workflow.hidden = true;
+                    workflow.setAttribute('aria-live', 'polite');
+                    workflow.innerHTML = `
+                        <p class="response-workflow-summary"></p>
+                        <div class="response-workflow-list"></div>
                     `;
-                    const diffTab = diffBox.querySelector('[data-response-view="diff"]');
-                    const previewTab = diffBox.querySelector('[data-response-view="preview"]');
-                    const diffView = diffBox.querySelector('.response-diff-box');
-                    const previewView = diffBox.querySelector('.response-preview-canvas');
-                    const setResponseView = (view) => {
-                        const showPreview = view === 'preview' && !previewTab.disabled;
-                        diffTab.classList.toggle('active', !showPreview);
-                        previewTab.classList.toggle('active', showPreview);
-                        diffView.hidden = showPreview;
-                        previewView.hidden = !showPreview;
-                    };
-                    diffTab.addEventListener('click', () => setResponseView('diff'));
-                    previewTab.addEventListener('click', () => setResponseView('preview'));
-                    contentDiv.appendChild(diffBox);
+                    contentDiv.appendChild(workflow);
+
+                    const fileCards = document.createElement('section');
+                    fileCards.className = 'response-file-cards';
+                    fileCards.hidden = true;
+                    fileCards.innerHTML = `
+                        <div class="response-file-card-list"></div>
+                        <button type="button" class="response-file-cards-download-all">
+                            <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><path d="M8 2v8m0 0 3-3m-3 3L5 7"/><path d="M2.5 11v1.5A1.5 1.5 0 0 0 4 14h8a1.5 1.5 0 0 0 1.5-1.5V11"/></svg>
+                            Download all
+                        </button>
+                    `;
+                    contentDiv.appendChild(fileCards);
                 }
 
                 body.appendChild(contentDiv);
@@ -1501,116 +1470,151 @@
             }
 
             let assistantFullText = '';
-            let codeResult = null; // { code, language, show_preview } — Code Mode only
-            const responseDiff = mode === 'code' ? assistantBubble.querySelector('.response-code-diff') : null;
-            const responseDiffTitle = responseDiff ? responseDiff.querySelector('.response-code-diff-title') : null;
-            const responseDiffCount = responseDiff ? responseDiff.querySelector('.response-code-diff-count') : null;
-            const responseDiffDot = responseDiff ? responseDiff.querySelector('.response-code-diff-dot') : null;
-            const responseDiffBox = responseDiff ? responseDiff.querySelector('.response-diff-box') : null;
-            const responsePreviewTab = responseDiff ? responseDiff.querySelector('[data-response-view="preview"]') : null;
-            const responsePreviewFrame = responseDiff ? responseDiff.querySelector('.response-preview-canvas iframe') : null;
-            const responseDiffState = { files: {}, order: [], additions: 0, lines: 0, status: 'Generating code' };
-
-            function renderResponseDiff() {
-                if (!responseDiff) return;
-                responseDiff.hidden = false;
-                responseDiffTitle.textContent = responseDiffState.status;
-                responseDiffCount.textContent = `${responseDiffState.order.length} file${responseDiffState.order.length === 1 ? '' : 's'} · ${responseDiffState.lines} line${responseDiffState.lines === 1 ? '' : 's'} · +${responseDiffState.additions}`;
-                responseDiffDot.classList.toggle('done', responseDiffState.status === 'Generated code');
-                responseDiffBox.replaceChildren();
-                if (!responseDiffState.order.length) {
-                    const empty = document.createElement('div');
-                    empty.className = 'response-diff-empty';
-                    empty.textContent = 'Waiting for generated code…';
-                    responseDiffBox.appendChild(empty);
-                    return;
+            let codeResult = null; // { code, language, files, activities, activity_summary } — Code Mode only
+            const workflowEl = mode === 'code' ? assistantBubble.querySelector('.response-workflow') : null;
+            const fileCardsEl = mode === 'code' ? assistantBubble.querySelector('.response-file-cards') : null;
+
+            const WORKFLOW_ICONS = {
+                edit: '<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><path d="M11.5 2.5a1.6 1.6 0 0 1 2.3 2.3L5.5 13 2 14l1-3.5 8.5-8Z"/></svg>',
+                view: '<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><path d="M4.5 3 2 5.5 4.5 8M11.5 3 14 5.5 11.5 8M9.5 2.5l-3 8"/></svg>',
+                note: '<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="8" cy="8" r="6"/><path d="M8 5v3l2 1.5"/></svg>',
+            };
+            const FILE_ICON = {
+                code: '<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><path d="M4.5 3 2 5.5 4.5 8M11.5 3 14 5.5 11.5 8M9.5 2.5l-3 8"/></svg>',
+                plain: '<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><path d="M4 1.5h5.5L13 5v9.5a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1v-12a1 1 0 0 1 1-1Z"/><path d="M9.5 1.5V5H13"/></svg>',
+            };
+            const CODE_EXTENSIONS = new Set(['html', 'htm', 'css', 'js', 'jsx', 'ts', 'tsx', 'py', 'json', 'java', 'c', 'cpp', 'go', 'rb', 'php', 'swift', 'rs', 'sql', 'sh']);
+
+            function escapeHtml(str) {
+                return String(str).replace(/[&<>"']/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
+            }
+            function inlineBackticks(str) {
+                return escapeHtml(str).replace(/`([^`]+)`/g, '<code>$1</code>');
+            }
+
+            function renderWorkflow(activities, summary) {
+                if (!workflowEl || !Array.isArray(activities) || !activities.length) return;
+                workflowEl.hidden = false;
+                const summaryEl = workflowEl.querySelector('.response-workflow-summary');
+                const listEl = workflowEl.querySelector('.response-workflow-list');
+                if (summary) {
+                    const parts = [];
+                    if (summary.commands) parts.push(`Ran ${summary.commands} command${summary.commands === 1 ? '' : 's'}`);
+                    if (summary.files_edited) parts.push(`edited ${summary.files_edited} file${summary.files_edited === 1 ? '' : 's'}`);
+                    if (summary.files_viewed) parts.push(`viewed ${summary.files_viewed} file${summary.files_viewed === 1 ? '' : 's'}`);
+                    let line = parts.join(', ');
+                    if (summary.notes) line += (line ? ' · ' : '') + `${summary.notes} note${summary.notes === 1 ? '' : 's'}`;
+                    summaryEl.textContent = line;
+                    summaryEl.style.display = line ? '' : 'none';
+                } else {
+                    summaryEl.style.display = 'none';
                 }
-                responseDiffState.order.forEach((key) => {
-                    const file = responseDiffState.files[key];
-                    const wrapper = document.createElement('div');
-                    wrapper.className = 'response-diff-file';
-                    const header = document.createElement('div');
-                    header.className = 'response-diff-file-header';
-                    const name = document.createElement('span');
-                    name.className = 'response-diff-file-name';
-                    name.textContent = file.filename || 'generated code';
-                    const stat = document.createElement('span');
-                    stat.className = 'response-diff-file-stat';
-                    stat.textContent = `+${file.lines || 0}`;
-                    header.append(name, stat);
-                    wrapper.appendChild(header);
-                    const pre = document.createElement('pre');
-                    pre.className = 'response-diff-code';
-                    const lines = (file.code || '').split('\n');
-                    if (!file.code) lines[0] = 'Writing…';
-                    lines.forEach((line) => {
-                        const row = document.createElement('span');
-                        row.className = 'response-diff-line added';
-                        row.textContent = line === 'Writing…' ? line : `+ ${line}`;
-                        pre.appendChild(row);
-                    });
-                    wrapper.appendChild(pre);
-                    responseDiffBox.appendChild(wrapper);
+                listEl.replaceChildren();
+                activities.forEach((activity) => {
+                    const row = document.createElement('div');
+                    row.className = `response-workflow-row ${activity.kind}`;
+                    const icon = document.createElement('span');
+                    icon.className = 'response-workflow-icon';
+                    icon.innerHTML = WORKFLOW_ICONS[activity.kind] || WORKFLOW_ICONS.note;
+                    row.appendChild(icon);
+                    const text = document.createElement('span');
+                    if (activity.kind === 'edit') {
+                        text.innerHTML = `Edited <span class="response-workflow-edit-file">${escapeHtml(activity.file || 'generated code')}</span>` +
+                            (activity.additions ? `<span class="response-workflow-add">+${activity.additions}</span>` : '') +
+                            (activity.deletions ? `<span class="response-workflow-del">-${activity.deletions}</span>` : '');
+                    } else if (activity.kind === 'note') {
+                        text.className = 'response-workflow-note-text';
+                        text.innerHTML = inlineBackticks(activity.text || '');
+                    } else {
+                        text.className = 'response-workflow-plain';
+                        text.innerHTML = inlineBackticks(activity.text || '');
+                    }
+                    row.appendChild(text);
+                    listEl.appendChild(row);
                 });
-                responseDiffBox.scrollTop = responseDiffBox.scrollHeight;
                 scrollToBottom();
             }
 
-            function startResponseDiffFile(filename = '') {
-                const key = filename || '__single__';
-                if (!responseDiffState.files[key]) {
-                    responseDiffState.order.push(key);
-                    responseDiffState.files[key] = { filename, code: '', lines: 0 };
-                }
-                renderResponseDiff();
+            function downloadTextFile(filename, content) {
+                const blob = new Blob([content || ''], { type: 'text/plain;charset=utf-8' });
+                const url = URL.createObjectURL(blob);
+                const a = document.createElement('a');
+                a.href = url;
+                a.download = filename || 'generated-file.txt';
+                document.body.appendChild(a);
+                a.click();
+                a.remove();
+                setTimeout(() => URL.revokeObjectURL(url), 1000);
             }
 
-            function appendResponseDiffDelta(delta, filename = '') {
-                if (!delta) return;
-                startResponseDiffFile(filename);
-                const file = responseDiffState.files[filename || '__single__'];
-                file.code += delta;
-                file.lines = file.code ? file.code.split('\n').length : 0;
-                responseDiffState.lines = responseDiffState.order.reduce((sum, name) => sum + responseDiffState.files[name].lines, 0);
-                responseDiffState.additions = responseDiffState.lines;
-                renderResponseDiff();
+            async function downloadAllFiles(entries) {
+                if (!entries.length) return;
+                if (entries.length === 1) {
+                    downloadTextFile(entries[0].filename, entries[0].content);
+                    return;
+                }
+                // Zip client-side when JSZip is available (no backend/workspace involved);
+                // otherwise fall back to triggering each download individually.
+                if (window.JSZip) {
+                    const zip = new JSZip();
+                    entries.forEach((entry) => zip.file(entry.filename, entry.content || ''));
+                    const blob = await zip.generateAsync({ type: 'blob' });
+                    const url = URL.createObjectURL(blob);
+                    const a = document.createElement('a');
+                    a.href = url;
+                    a.download = 'generated-code.zip';
+                    document.body.appendChild(a);
+                    a.click();
+                    a.remove();
+                    setTimeout(() => URL.revokeObjectURL(url), 1000);
+                } else {
+                    entries.forEach((entry) => downloadTextFile(entry.filename, entry.content));
+                }
             }
 
-            function finalizeResponseDiff(result = {}) {
-                if (!responseDiff) return;
+            function renderFileCards(result = {}) {
+                if (!fileCardsEl) return;
                 const hasFiles = result.files && Object.keys(result.files).length > 0;
-                const entries = hasFiles ? Object.entries(result.files) : (result.code ? [['__single__', result.code]] : []);
-                entries.forEach(([filename, code]) => {
-                    startResponseDiffFile(filename === '__single__' ? '' : filename);
-                    const file = responseDiffState.files[filename];
-                    file.code = String(code || '');
-                    file.lines = file.code ? file.code.split('\n').length : 0;
-                });
-                responseDiffState.lines = responseDiffState.order.reduce((sum, name) => sum + responseDiffState.files[name].lines, 0);
-                responseDiffState.additions = responseDiffState.lines;
-                responseDiffState.status = 'Generated code';
-                renderResponseDiff();
-
-                const previewable = ['html', 'htm', 'css', 'jsx', 'tsx', 'js', 'javascript', 'react'];
-                let previewEntry = null;
-                let previewLanguage = '';
-                for (const [filename, code] of entries) {
-                    const language = hasFiles ? ((result.file_languages && result.file_languages[filename]) || '') : (result.language || '');
-                    const extension = (filename || '').split('.').pop().toLowerCase();
-                    if (previewable.includes(language.toLowerCase()) || previewable.includes(extension)) {
-                        previewEntry = [filename, code];
-                        previewLanguage = language || extension;
-                        break;
-                    }
-                }
-                if (!previewEntry && entries.length === 1 && result.language) {
-                    previewEntry = entries[0];
-                    previewLanguage = result.language;
-                }
-                if (responsePreviewTab && responsePreviewFrame && previewEntry) {
-                    responsePreviewFrame.srcdoc = buildPreviewDoc(previewLanguage, previewEntry[1]);
-                    responsePreviewTab.disabled = false;
+                const entries = hasFiles
+                    ? Object.entries(result.files).map(([filename, content]) => ({
+                        filename,
+                        content: String(content || ''),
+                        language: (result.file_languages && result.file_languages[filename]) || '',
+                    }))
+                    : (result.code ? [{ filename: `generated.${result.language || 'txt'}`, content: String(result.code), language: result.language || '' }] : []);
+                if (!entries.length) {
+                    fileCardsEl.hidden = true;
+                    return;
                 }
+                fileCardsEl.hidden = false;
+                const listEl = fileCardsEl.querySelector('.response-file-card-list');
+                listEl.replaceChildren();
+                entries.forEach((entry) => {
+                    const extension = (entry.filename.split('.').pop() || '').toLowerCase();
+                    const isCode = CODE_EXTENSIONS.has(extension) || CODE_EXTENSIONS.has((entry.language || '').toLowerCase());
+                    const card = document.createElement('div');
+                    card.className = 'response-file-card';
+                    card.innerHTML = `
+                        <span class="response-file-card-icon">${isCode ? FILE_ICON.code : FILE_ICON.plain}</span>
+                        <span class="response-file-card-meta">
+                            <div class="response-file-card-name"></div>
+                            <div class="response-file-card-type">${isCode ? `Code · ${extension.toUpperCase()}` : extension.toUpperCase()}</div>
+                        </span>
+                        <button type="button" class="response-file-card-download">Download</button>
+                    `;
+                    card.querySelector('.response-file-card-name').textContent = entry.filename;
+                    card.querySelector('.response-file-card-download').addEventListener('click', () => downloadTextFile(entry.filename, entry.content));
+                    listEl.appendChild(card);
+                });
+                const downloadAllBtn = fileCardsEl.querySelector('.response-file-cards-download-all');
+                downloadAllBtn.style.display = entries.length > 1 ? '' : 'none';
+                downloadAllBtn.onclick = () => downloadAllFiles(entries);
+                scrollToBottom();
+            }
+
+            function finalizeCodeResult(result = {}) {
+                renderWorkflow(result.activities, result.activity_summary);
+                renderFileCards(result);
             }
 
             // Lives outside .message-prose so setProse() re-parsing the prose HTML
@@ -1660,13 +1664,12 @@
                                     appendLiveThought(parsed.text);
                                 } else if (parsed.type === 'RETRY' || parsed.type === 'ERROR') {
                                     addThought(parsed.type === 'ERROR' ? 'Code generation error' : 'Retrying', parsed.message || parsed.reason || '', parsed.type.toLowerCase());
-                                } else if (parsed.type === 'code_start' || parsed.type === 'code_file_start') {
-                                    if (mode === 'code') startResponseDiffFile(parsed.filename || '');
-                                } else if (parsed.type === 'code_delta') {
-                                    if (mode === 'code') appendResponseDiffDelta(parsed.delta, parsed.filename || '');
+                                } else if (parsed.type === 'code_start' || parsed.type === 'code_file_start' || parsed.type === 'code_delta') {
+                                    // Raw code text is no longer visualized live — the workflow trace
+                                    // and file cards render once from the final code_result instead.
                                 } else if (parsed.type === 'code_result') {
                                     codeResult = parsed;
-                                    if (mode === 'code') finalizeResponseDiff(parsed);
+                                    if (mode === 'code') finalizeCodeResult(parsed);
                                 } else if (parsed.type === 'message_reset') {
                                     // Compatibility event: clear the current draft if a server
                                     // explicitly asks the client to reset it.
@@ -1702,7 +1705,7 @@
                     if (mode === 'code' && hasCodeResult) {
                         setProse(textContainer, marked.parse(normalizeMathDelimiters(stripCodeFence(assistantFullText))));
                         highlightCode(textContainer);
-                        finalizeResponseDiff(codeResult);
+                        finalizeCodeResult(codeResult);
                     } else {
                         setProse(textContainer, marked.parse(normalizeMathDelimiters(assistantFullText)));
                         highlightCode(textContainer);
@@ -1724,7 +1727,7 @@
                     if (mode === 'code' && hasCode) {
                         setProse(textContainer, marked.parse(normalizeMathDelimiters(stripCodeFence(assistantFullText))));
                         highlightCode(textContainer);
-                        finalizeResponseDiff(data);
+                        finalizeCodeResult(data);
                     } else {
                         setProse(textContainer, marked.parse(normalizeMathDelimiters(assistantFullText)));
                         highlightCode(textContainer);
