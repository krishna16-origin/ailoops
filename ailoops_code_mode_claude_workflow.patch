--- a/code_agent.py
+++ b/code_agent.py
@@
-    turn_files_touched: Dict[str, str] = {}
+    turn_files_touched: Dict[str, str] = {}
+    command_count = 0
     final_text = ""
     reached_final = False
@@
-        if thought:
-            await emit({"type": "agent_message", "text": thought})
-            activities.append({"kind": _classify_note_kind(thought), "text": thought})
+        if thought:
+            thought_kind = _classify_note_kind(thought)
+            await emit({"type": "agent_message", "text": thought, "kind": thought_kind})
+            activities.append({"kind": thought_kind, "text": thought})
@@
         if action == "read_file":
+            command_count += 1
             existing = file_store.get(path)
@@
         if action in ("edit_file", "create_file"):
+            command_count += 1
             content = turn["content"] or ""
@@
             if is_edit:
                 additions, deletions, diff_lines = diff_file(old_content, content)
-                activities.append({"kind": "command", "text": f"diff -u {path}"})
             else:
@@
         if action == "delete_file":
+            command_count += 1
             existed = path in file_store
             file_store.pop(path, None)
-            activities.append({"kind": "command", "text": f"rm {path}"})
@@
-    commands = sum(1 for a in activities if a["kind"] == "command")
+    commands = command_count


--- a/frontend/index.html
+++ b/frontend/index.html
@@
                                 } else if (parsed.type === 'thought') {
                                     appendLiveThought(parsed.text);
+                                } else if (parsed.type === 'agent_message' && mode === 'code') {
+                                    if (workflowEl && parsed.text) {
+                                        workflowEl.hidden = false;
+                                        workflowEl.querySelector('.response-workflow-list').appendChild(
+                                            createWorkflowRow({
+                                                kind: parsed.kind || 'note',
+                                                text: parsed.text
+                                            })
+                                        );
+                                        scrollToBottom();
+                                    }
+                                } else if (parsed.type === 'activity_start' && mode === 'code') {
+                                    if (workflowEl) {
+                                        workflowEl.hidden = false;
+                                        const list = workflowEl.querySelector('.response-workflow-list');
+                                        const action = parsed.action;
+
+                                        if (action === 'read') {
+                                            list.appendChild(
+                                                createWorkflowRow({
+                                                    kind: 'view',
+                                                    text: `Read ${parsed.file || ''}`
+                                                })
+                                            );
+                                        } else if (action === 'edit' || action === 'create') {
+                                            const key = parsed.file || '';
+                                            const row = createWorkflowRow({
+                                                kind: 'edit',
+                                                file: key || 'generated code',
+                                                filename: key,
+                                                additions: 0,
+                                                deletions: 0
+                                            });
+                                            workflowRowsByFile.set(key, row);
+                                            list.appendChild(row);
+                                        } else if (action === 'delete') {
+                                            list.appendChild(
+                                                createWorkflowRow({
+                                                    kind: 'note',
+                                                    text: `Deleted ${parsed.file || ''}`
+                                                })
+                                            );
+                                        }
+
+                                        scrollToBottom();
+                                    }
                                 } else if (parsed.type === 'RETRY' || parsed.type === 'ERROR') {
@@
-                                    // Also grow the activity feed itself live — an "Editing <file>…"
-                                    // row appears the instant the file starts, exactly like a Claude
-                                    // Code session narrating each edit as it happens, instead of the
-                                    // whole feed only popping in at the very end of the turn.
-                                    if (workflowEl && !workflowRowsByFile.has(key)) {
-                                        workflowEl.hidden = false;
-                                        const row = createWorkflowRow({ kind: 'edit', file: canvasFileLabel(key), filename: key, additions: 0, deletions: 0 });
-                                        workflowRowsByFile.set(key, row);
-                                        workflowEl.querySelector('.response-workflow-list').appendChild(row);
-                                        scrollToBottom();
-                                    }
+                                    // The workflow row is created from activity_start so
+                                    // THOUGHT -> EDIT ordering remains exact. This event
+                                    // only prepares the live diff/preview tab.
