diff --git a/app.py b/app.py
index cc8bee5..ca1df1c 100644
--- a/app.py
+++ b/app.py
@@ -4195,63 +4195,6 @@ async def run_code_graph_streaming(initial_state: dict, timeout: float, token_qu
                 pass
         await agen.aclose()
 
-async def stream_code_thinking(model_key: str, temperature: float, message: str, history: List[BaseMessage]):
-    """Streams a genuine reasoning trace token-by-token before the coding loop starts — the
-    Claude.ai-style 'Thinking' panel. This is a plain free-text call (no Pydantic parser), with
-    thinking turned ON, so the entire token budget goes toward real reasoning instead of
-    competing with a JSON schema the way it would inside execute_code_llm_structured (that's the
-    documented reason thinking is forced OFF for every structured call — see CODE_THINKING_OFF_KWARGS
-    above). Kept on its own model instance so it never eats into the 32K budget reserved for the
-    actual code-generating calls.
-
-    The prompt deliberately asks for real structure (candidate interpretations weighed against
-    each other, then a plain-markdown breakdown of what/how/risks) rather than a single flat
-    paragraph — this is what makes the panel read like an engineer actually working through the
-    problem instead of a decorative loading message. The frontend renders this as plain text on
-    purpose (see appendLiveThought), so the markdown markers below (**bold**, numbered/bulleted
-    lists, `code`) show up as literal characters, exactly like Claude's own raw thinking trace —
-    not as a request to prettify the output, just to make the model's actual reasoning legible."""
-    model_name = CODE_MODEL_MAP.get((model_key or "").strip().lower(), CODE_MODEL_MAP["kimi"])
-    thinking_llm = ChatNVIDIA(
-        model=model_name,
-        temperature=temperature,
-        max_tokens=6144,
-        timeout=75,
-        model_kwargs={"chat_template_kwargs": {"thinking": True, "enable_thinking": True}},
-    )
-    context = "\n".join([f"{m.type.capitalize()}: {m.content}" for m in history[-6:]])
-    prompt = (
-        f"Current Date & Time: {get_current_datetime_str()}\n\n"
-        f"{context}\n\nUser's new request: {message}\n\n"
-        "Think this through out loud, in your own words, the way you'd actually reason through a "
-        "real engineering task before touching an editor — don't write final code yet, and don't "
-        "just restate the request in different words.\n\n"
-        "Structure it for real, using plain markdown as you go (not because it needs to look "
-        "pretty, but because that's how you'd actually lay out your own thinking):\n\n"
-        "1. If the request is ambiguous, underspecified, could plausibly mean more than one "
-        "thing, or contains something that looks like a typo — say so directly and name the "
-        "**specific candidates** you're weighing (a short numbered or bulleted list, one bold "
-        "label per candidate), then reason about which is most likely given the context and commit "
-        "to one. If it's already unambiguous, skip straight past this instead of manufacturing "
-        "false uncertainty.\n"
-        "2. Once you've landed on what's actually being built, break the thinking into real "
-        "sections with bold or heading-style labels — e.g. **What it needs:** (the concrete "
-        "requirements, data shape, interactions), **Technical approach:** (single file vs several, "
-        "the concrete implementation plan, key libraries or patterns), and **Risks / tricky "
-        "parts:** (the specific part most likely to go wrong or need the most care, and why).\n"
-        "3. Be concrete and specific — name real fields, functions, edge cases, and trade-offs "
-        "instead of vague filler like 'handle errors properly' or 'make it look nice'.\n\n"
-        "Write it as continuous reasoning, not a formal report — the structure should feel like "
-        "how your own thoughts are actually organized, not a template you're filling in."
-    )
-    try:
-        async for chunk in thinking_llm.astream(prompt):
-            piece = getattr(chunk, "content", "") or ""
-            if piece:
-                yield piece
-    except Exception as e:
-        print(f"[CodeMode] Thinking stream failed (non-fatal, coding loop still runs): {e}")
-
 def iter_code_chunks(text: str, target_chunks: int = 60, min_chunk: int = 24, max_chunk: int = 800):
     """Splits an already-generated code string into pieces for progressive delivery to the
     code canvas — a Claude/Gemini-Canvas-style 'typewriter' fill instead of the whole file
@@ -4283,19 +4226,11 @@ async def generate_code_stream(request: "CodeChatRequest", session: dict, sessio
         final_response, code, language, is_frontend = result["text"], result["code"], result["language"], result["is_frontend"]
         final_state = {"completion_score": 100, "iteration": 1}
     else:
-        # Real Claude.ai-style thinking: stream the model's actual reasoning token-by-token
-        # BEFORE the structured plan/execute loop starts, as its own SSE event type. The
-        # frontend renders these `thinking` deltas into a collapsible panel and, on
-        # `thinking_done`, headers it "Thought for {elapsed}s" — same shape as Claude.ai.
-        yield f"data: {json.dumps({'type': 'thinking_start'})}\n\n"
-        thinking_start = asyncio.get_event_loop().time()
-        thinking_text = ""
-        async for piece in stream_code_thinking(request.model, request.temperature, request.message, session["messages"]):
-            thinking_text += piece
-            yield f"data: {json.dumps({'type': 'thinking', 'delta': piece})}\n\n"
-        thinking_elapsed = round(asyncio.get_event_loop().time() - thinking_start, 1)
-        yield f"data: {json.dumps({'type': 'thinking_done', 'elapsed': thinking_elapsed})}\n\n"
-
+        # The "Thinking" panel is driven entirely by the real per-node status/activity
+        # events emitted below as the plan/execute loop actually runs (Understanding,
+        # Planning, writing each file, reviewing, etc.) — there's no separate free-text
+        # reasoning pass before it anymore, so the first thing the user sees is genuine
+        # backend progress instead of a preliminary "thinking out loud" essay.
         initial_state = {
             "messages": session["messages"],
             "model_key": request.model,
@@ -4535,7 +4470,7 @@ async def generate_code_stream(request: "CodeChatRequest", session: dict, sessio
     }
     yield f"data: {json.dumps({'type': 'turn_summary', **summary, 'session_id': session_id})}\n\n"
 
-    yield f"data: {json.dumps({'type': 'code_result', 'code': code, 'language': language, 'files': files, 'show_preview': bool(is_frontend), 'session_id': session_id})}\n\n"
+    yield f"data: {json.dumps({'type': 'code_result', 'code': code, 'language': language, 'files': files, 'file_languages': final_state.get('file_languages', {}), 'show_preview': bool(is_frontend), 'session_id': session_id})}\n\n"
 
 # ----------------------------------------------------------------------
 # CODE MODE: FastAPI request models
@@ -4706,6 +4641,7 @@ async def _handle_code_chat(request: CodeChatRequest):
         "code": code,
         "language": language,
         "files": files,
+        "file_languages": final_state.get("file_languages", {}),
         "show_preview": bool(is_frontend),   # frontend code -> True (render live preview); backend code -> False
         "model": request.model,
         "reasoning_level": request.reasoning_level,
diff --git a/frontend/index.html b/frontend/index.html
index 1d894f7..851ebe5 100644
--- a/frontend/index.html
+++ b/frontend/index.html
@@ -182,20 +182,16 @@
         .message-text .artifact-chip-title { font-size:13.5px; font-weight:500; color:var(--text-primary); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
         .message-text .artifact-chip-meta { font-size:12px; color:var(--text-secondary); margin-top:2px; }
         .message-text .artifact-chip-btn { flex-shrink:0; background-color:var(--text-primary); color:var(--bg-main); border:none; border-radius:999px; font-size:12.5px; font-weight:600; padding:6px 14px; cursor:pointer; pointer-events:none; }
-        /* Small live-fill box shown INSIDE the chat message while code is being generated —
-           the chat-visible counterpart to the canvas stream. Fixed height with internal
-           scroll: it never grows the chat bubble no matter how large the file gets, it just
-           tails the latest lines like a small log window. Replaced by .artifact-chip once
-           the file is complete (see the code_result handler). */
-        .message-text .inline-code-stream { margin:14px 0; border:1px solid var(--border-color); border-radius:12px; overflow:hidden; background-color:#111; max-width:560px; }
-        .message-text .inline-code-stream-header { display:flex; align-items:center; gap:8px; padding:8px 12px; background-color:#1c1c1c; border-bottom:1px solid var(--border-color); }
-        .message-text .inline-code-stream-header svg { width:14px; height:14px; color:var(--accent); flex-shrink:0; }
-        .message-text .inline-code-stream-filename { font-size:12.5px; font-weight:500; color:var(--text-primary); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
-        .message-text .inline-code-stream-status { font-size:11.5px; color:var(--text-secondary); margin-left:auto; flex-shrink:0; display:flex; align-items:center; gap:6px; }
-        .message-text .inline-code-stream-status .dot { width:6px; height:6px; border-radius:50%; background-color:var(--accent); animation:inlineStreamPulse 1s ease-in-out infinite; }
+        /* Small status-only chip shown INSIDE the chat message while a file is being
+           generated — names the file and shows a pulsing "writing" dot, but never renders
+           the code itself in the chat; the live code only ever appears in the canvas panel.
+           Replaced by .artifact-chip once the file (or the whole multi-file build) is
+           complete (see the code_result handler). */
+        .message-text .inline-code-status { display:flex; align-items:center; gap:8px; margin:14px 0; padding:8px 12px; border:1px solid var(--border-color); border-radius:10px; background-color:#1c1c1c; max-width:320px; }
+        .message-text .inline-code-status svg { width:14px; height:14px; color:var(--accent); flex-shrink:0; }
+        .message-text .inline-code-status-filename { font-size:12.5px; font-weight:500; color:var(--text-primary); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
+        .message-text .inline-code-status-dot { width:6px; height:6px; border-radius:50%; background-color:var(--accent); margin-left:auto; flex-shrink:0; animation:inlineStreamPulse 1s ease-in-out infinite; }
         @keyframes inlineStreamPulse { 0%,100% { opacity:0.3; } 50% { opacity:1; } }
-        .message-text .inline-code-stream-body { margin:0; padding:10px 14px; max-height:320px; overflow:auto; }
-        .message-text .inline-code-stream-body code { font-size:12.5px; line-height:1.6; font-family:'Courier New', Courier, monospace; color:var(--text-secondary); white-space:pre; }
         .message-text .copy-btn { display:flex; align-items:center; gap:6px; background:transparent; border:none; color:var(--text-secondary); font-size:12px; cursor:pointer; padding:4px 8px; border-radius:6px; transition:background 0.2s, color 0.2s; }
         .message-text .copy-btn:hover { background-color:var(--bg-hover); color:var(--text-primary); }
         .message-text .copy-btn svg { width:14px; height:14px; }
@@ -337,6 +333,14 @@
         .canvas-icon-btn { background:transparent; border:none; color:var(--text-secondary); cursor:pointer; padding:6px; border-radius:6px; display:flex; align-items:center; justify-content:center; transition:background 0.2s ease, color 0.2s ease; }
         .canvas-icon-btn:hover { background-color:var(--bg-hover); color:var(--text-primary); }
         .canvas-icon-btn svg { width:16px; height:16px; }
+        /* File-switcher strip along the top of the canvas — only shown once a build has
+           produced more than one file, so a single-file result looks exactly as before. */
+        .canvas-file-tabs { display:flex; align-items:center; gap:2px; padding:6px 10px; border-bottom:1px solid var(--border-color); overflow-x:auto; flex-shrink:0; }
+        .canvas-file-tabs[hidden] { display:none; }
+        .canvas-file-tab { display:flex; align-items:center; gap:6px; border:none; background:transparent; color:var(--text-secondary); font-size:12.5px; font-family:'Courier New', Courier, monospace; padding:6px 10px; border-radius:8px; cursor:pointer; white-space:nowrap; flex-shrink:0; transition:background 0.2s ease, color 0.2s ease; }
+        .canvas-file-tab:hover { background-color:var(--bg-hover); color:var(--text-primary); }
+        .canvas-file-tab.active { background-color:var(--bg-input); color:var(--text-primary); }
+        .canvas-file-tab .canvas-file-tab-dot { width:6px; height:6px; border-radius:50%; background-color:var(--accent); flex-shrink:0; animation:inlineStreamPulse 1s ease-in-out infinite; }
         .canvas-body { flex:1; min-height:0; position:relative; }
         .canvas-view { width:100%; height:100%; overflow:auto; }
         .canvas-view[hidden] { display:none; }
@@ -544,6 +548,7 @@
                 </button>
             </div>
         </div>
+        <div class="canvas-file-tabs" id="canvasFileTabs" hidden></div>
         <div class="canvas-body">
             <div class="canvas-view" id="canvasPreviewView">
                 <iframe id="canvasPreviewFrame" sandbox="allow-scripts allow-same-origin allow-forms allow-modals"></iframe>
@@ -730,7 +735,6 @@
                 hljs.highlightElement(codeEl);
                 const pre = codeEl.parentElement;
                 if (pre.parentElement.classList.contains('code-card')) return;
-                if (pre.classList.contains('inline-code-stream-body')) return;
 
                 const langClass = [...codeEl.classList].find(c => c.startsWith('language-'));
                 const lang = langClass ? langClass.replace('language-', '') : 'text';
@@ -793,47 +797,43 @@
             });
         }
 
-        // ================= INLINE CODE STREAM (chat-visible counterpart to the canvas) =================
+        // ================= INLINE CODE STATUS (chat-visible, status only — never shows code) =================
         // Renders directly inside the assistant's message bubble, as a sibling of .message-prose,
-        // so re-rendering the prose on every streamed token never wipes it out. This box exists
-        // ONLY to show code being written live, token by token, while it streams. The instant
-        // generation finishes it's removed — the finished file lives in the canvas panel (and the
-        // artifact chip below), exactly as before this box existed.
-
-        // Called the moment code_start / code_file_start arrives — creates the box with a
-        // pulsing "Writing code…" status in the header and an empty body ready to be filled.
-        function startInlineCodeStream(container, language, filename) {
-            const card = document.createElement('div');
-            card.className = 'inline-code-stream';
-            card.innerHTML = `
-                <div class="inline-code-stream-header">
-                    <span class="inline-code-stream-filename">${filename || friendlyLangLabel(language) || 'Code'}</span>
-                    <span class="inline-code-stream-status"><span class="dot"></span>Writing code…</span>
-                </div>
-                <pre class="inline-code-stream-body"><code></code></pre>
+        // so re-rendering the prose on every streamed token never wipes it out. Names whichever
+        // file is currently being written, with a pulsing dot — the actual code is never shown
+        // here, only in the canvas panel. Replaced by the artifact chip once generation finishes.
+        function startInlineCodeStatus(container, language, filename) {
+            const chip = document.createElement('div');
+            chip.className = 'inline-code-status';
+            chip.innerHTML = `
+                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 18 22 12 16 6"></polyline><polyline points="8 6 2 12 8 18"></polyline></svg>
+                <span class="inline-code-status-filename">Writing ${escapeHtml(filename || friendlyLangLabel(language) || 'code')}…</span>
+                <span class="inline-code-status-dot"></span>
             `;
-            container.appendChild(card);
-            return { card, codeEl: card.querySelector('code'), bodyEl: card.querySelector('.inline-code-stream-body'), language };
+            container.appendChild(chip);
+            return chip;
         }
 
-        // Called on every code_delta — appends the new text and keeps the box scrolled to the
-        // latest line, like a small typewriter tailing the file as it's generated.
-        function appendInlineCodeStream(state, delta) {
-            if (!state || !delta) return;
-            state.codeEl.textContent += delta;
-            state.bodyEl.scrollTop = state.bodyEl.scrollHeight;
+        // Called on code_file_start for every file after the first, in a multi-file build —
+        // updates which filename the status line names, without ever creating a second chip.
+        function updateInlineCodeStatus(chip, language, filename) {
+            if (!chip) return;
+            const label = chip.querySelector('.inline-code-status-filename');
+            if (label) label.textContent = `Writing ${filename || friendlyLangLabel(language) || 'code'}…`;
         }
 
-        // Called once a file is done streaming (next file starting, or the whole response
-        // complete) — just removes the live box. Nothing about the finished code is rendered
-        // here; updateCodeCanvas() + appendArtifactChip() (called separately) take over.
-        function removeInlineCodeStream(state) {
-            if (state && state.card) state.card.remove();
+        // Called once the whole response is done — just removes the live status chip. Nothing
+        // about the finished code is rendered here; appendArtifactChip() takes over.
+        function removeInlineCodeStatus(chip) {
+            if (chip) chip.remove();
         }
 
         // ================= CODE CANVAS =================
-        // Everything below only runs when Code mode calls updateCodeCanvas().
-        // Nothing here is invoked from the Chat mode code path.
+        // Everything below only runs when Code mode calls updateCodeCanvas(). Tracks every file
+        // a build produces in modes.code.canvas.files (keyed by filename, ordered via .order),
+        // not just the last one written, with a tab strip to switch between them — this is what
+        // makes a multi-file build behave like Claude's own multi-file artifacts instead of each
+        // new file silently overwriting the last.
 
         function friendlyLangLabel(language) {
             const lang = (language || '').trim();
@@ -847,12 +847,60 @@
             return map[(language || '').trim().toLowerCase()] || 'txt';
         }
 
-        // Live-fills the code canvas as code_delta events arrive, the same way the
-        // thinking panel fills token-by-token — never touches the chat bubble. The panel
-        // itself has a fixed height with internal scrolling (see .canvas-view CSS), so this
-        // only ever scrolls the code view; it can't make the box grow.
+        // Whether a given file can be rendered live in the preview iframe (buildPreviewDoc
+        // supports html/css/jsx/tsx/plain-js) — checked per file, so in a multi-file build a
+        // stylesheet or script tab simply has no Preview button while the HTML tab does.
+        function fileCanPreview(filename, language) {
+            const previewable = ['html', 'htm', 'css', 'jsx', 'tsx', 'js', 'javascript', 'react'];
+            const lang = (language || '').trim().toLowerCase();
+            const ext = (filename || '').split('.').pop().trim().toLowerCase();
+            return previewable.includes(lang) || previewable.includes(ext);
+        }
+
+        function renderCanvasFileTabs() {
+            const state = modes.code.canvas;
+            const bar = document.getElementById('canvasFileTabs');
+            if (!state || state.order.length <= 1) { bar.hidden = true; bar.innerHTML = ''; return; }
+            bar.hidden = false;
+            bar.innerHTML = state.order.map(key => {
+                const file = state.files[key];
+                const label = file.filename || friendlyLangLabel(file.language);
+                const isBeingWritten = streamingActive && key === state.liveFile;
+                return `<button type="button" class="canvas-file-tab${key === state.activeFile ? ' active' : ''}" data-file="${escapeHtml(key)}">` +
+                    `${escapeHtml(label)}${isBeingWritten ? '<span class="canvas-file-tab-dot"></span>' : ''}</button>`;
+            }).join('');
+            bar.querySelectorAll('.canvas-file-tab').forEach(btn => {
+                btn.addEventListener('click', () => setActiveCanvasFile(btn.dataset.file));
+            });
+        }
+
+        function setActiveCanvasFile(key) {
+            const state = modes.code.canvas;
+            if (!state || !state.files[key]) return;
+            state.activeFile = key;
+            state.tab = 'code';
+            renderCodeCanvas();
+        }
+
+        // Live-fills the code canvas as code_delta events arrive, the same way the thinking
+        // panel fills token-by-token — never touches the chat bubble. Called once per file, the
+        // moment code_start/code_file_start arrives for it; keeps every earlier file's content
+        // in state.files instead of discarding it, and auto-follows the tab strip onto whichever
+        // file is currently being written, exactly like watching a multi-file artifact get built.
         function startCodeCanvasStream(language, filename) {
             streamingActive = true;
+            let state = modes.code.canvas;
+            if (!state || !state.files) {
+                state = { open: true, files: {}, order: [], activeFile: null, liveFile: null, tab: 'code' };
+                modes.code.canvas = state;
+            }
+            const key = filename || '__single__';
+            if (!(key in state.files)) state.order.push(key);
+            state.files[key] = { language: language || '', code: '', filename: filename || '' };
+            state.activeFile = key;
+            state.liveFile = key;
+
+            renderCanvasFileTabs();
             canvasTitle.textContent = filename || friendlyLangLabel(language) || 'Code';
             canvasCodeEl.textContent = '';
             canvasCodeEl.className = `language-${(language || 'plaintext').toLowerCase()}`;
@@ -862,45 +910,71 @@
             codeCanvas.hidden = false;
         }
 
-        function appendCodeCanvasStream(delta) {
+        function appendCodeCanvasStream(delta, filename) {
             if (!delta) return;
-            canvasCodeEl.textContent += delta;
-            const view = document.getElementById('canvasCodeView');
-            view.scrollTop = view.scrollHeight; // keep the "typewriter" pinned to the latest line
+            const state = modes.code.canvas;
+            if (!state) return;
+            const key = filename || '__single__';
+            const file = state.files[key];
+            if (!file) return;
+            file.code += delta;
+            if (state.activeFile === key) {
+                canvasCodeEl.textContent += delta;
+                const view = document.getElementById('canvasCodeView');
+                view.scrollTop = view.scrollHeight; // keep the "typewriter" pinned to the latest line
+            }
         }
 
-        // Called from Code mode's sendMessage() the moment a code_result arrives
-        // (streaming) or a non-streaming response with code comes back. Stores the
-        // result on modes.code and renders it only if Code mode is currently visible.
-        function updateCodeCanvas(language, code, showPreview) {
-            if (!code) return;
+        // Called from Code mode's sendMessage() once the final code_result arrives (streaming)
+        // or a non-streaming response with code comes back. `files`/`fileLanguages` are
+        // {filename: value} maps for multi-file builds; a single-file result just has
+        // language/code instead. Always reconciles the canvas against this authoritative
+        // payload, so a dropped or out-of-order delta can never leave it showing something
+        // wrong or partial. Stores the result on modes.code and renders it only if Code mode is
+        // currently visible.
+        function updateCodeCanvas({ language, code, files, fileLanguages, showPreview } = {}) {
+            const hasFiles = files && Object.keys(files).length > 0;
+            if (!hasFiles && !code) return;
             streamingActive = false;
-            modes.code.canvas = {
-                open: true,
-                language: language || '',
-                code: code,
-                showPreview: !!showPreview,
-                tab: showPreview ? 'preview' : 'code'
-            };
+
+            const state = { open: true, files: {}, order: [], activeFile: null, liveFile: null, tab: 'code' };
+            if (hasFiles) {
+                for (const [fname, fcode] of Object.entries(files)) {
+                    const flang = (fileLanguages && fileLanguages[fname]) || '';
+                    state.files[fname] = { language: flang, code: fcode, filename: fname };
+                    state.order.push(fname);
+                }
+                state.activeFile = state.order[0];
+            } else {
+                state.files['__single__'] = { language: language || '', code, filename: '', forcePreview: !!showPreview };
+                state.order.push('__single__');
+                state.activeFile = '__single__';
+            }
+            modes.code.canvas = state;
             if (currentMode === 'code') renderCodeCanvas();
         }
 
         function renderCodeCanvas() {
             const state = modes.code.canvas;
-            if (!state || !state.open) { codeCanvas.hidden = true; return; }
+            if (!state || !state.open || !state.activeFile) { codeCanvas.hidden = true; return; }
+            const file = state.files[state.activeFile];
+            if (!file) { codeCanvas.hidden = true; return; }
 
-            canvasTitle.textContent = `${friendlyLangLabel(state.language)}${state.showPreview ? ' Preview' : ''}`;
-            canvasCodeEl.textContent = state.code;
-            canvasCodeEl.className = `language-${(state.language || 'plaintext').toLowerCase()}`;
+            renderCanvasFileTabs();
+
+            const canPreview = fileCanPreview(file.filename, file.language) || file.forcePreview;
+            canvasTitle.textContent = file.filename || `${friendlyLangLabel(file.language)}${canPreview ? ' Preview' : ''}`;
+            canvasCodeEl.textContent = file.code;
+            canvasCodeEl.className = `language-${(file.language || 'plaintext').toLowerCase()}`;
             canvasCodeEl.removeAttribute('data-highlighted');
             hljs.highlightElement(canvasCodeEl);
 
-            document.getElementById('canvasPreviewTabBtn').style.display = state.showPreview ? '' : 'none';
-            if (state.showPreview) {
-                canvasPreviewFrame.srcdoc = buildPreviewDoc(state.language, state.code);
+            document.getElementById('canvasPreviewTabBtn').style.display = canPreview ? '' : 'none';
+            if (canPreview) {
+                canvasPreviewFrame.srcdoc = buildPreviewDoc(file.language, file.code);
             }
 
-            setCanvasTab(state.showPreview ? state.tab : 'code');
+            setCanvasTab(canPreview ? state.tab : 'code');
             codeCanvas.hidden = false;
         }
 
@@ -920,22 +994,27 @@
             codeCanvas.hidden = true;
         });
 
-        document.getElementById('canvasDownloadBtn').addEventListener('click', () => {
+        function activeCanvasFile() {
             const state = modes.code.canvas;
-            if (!state || !state.code) return;
-            const blob = new Blob([state.code], { type: 'text/plain' });
+            return state && state.activeFile ? state.files[state.activeFile] : null;
+        }
+
+        document.getElementById('canvasDownloadBtn').addEventListener('click', () => {
+            const file = activeCanvasFile();
+            if (!file || !file.code) return;
+            const blob = new Blob([file.code], { type: 'text/plain' });
             const url = URL.createObjectURL(blob);
             const a = document.createElement('a');
             a.href = url;
-            a.download = `code.${fileExtensionFor(state.language)}`;
+            a.download = file.filename || `code.${fileExtensionFor(file.language)}`;
             a.click();
             URL.revokeObjectURL(url);
         });
 
         document.getElementById('canvasCopyBtn').addEventListener('click', () => {
-            const state = modes.code.canvas;
-            if (!state || !state.code) return;
-            navigator.clipboard.writeText(state.code);
+            const file = activeCanvasFile();
+            if (!file || !file.code) return;
+            navigator.clipboard.writeText(file.code);
             const btn = document.getElementById('canvasCopyBtn');
             const original = btn.title;
             btn.title = 'Copied!';
@@ -943,39 +1022,49 @@
         });
 
         document.getElementById('canvasOpenNewTabBtn').addEventListener('click', () => {
-            const state = modes.code.canvas;
-            if (!state || !state.showPreview) return;
+            const file = activeCanvasFile();
+            if (!file) return;
             const win = window.open('', '_blank');
             if (!win) return;
             win.document.open();
-            win.document.write(buildPreviewDoc(state.language, state.code));
+            win.document.write(buildPreviewDoc(file.language, file.code));
             win.document.close();
         });
 
-        // Removes the fenced code block from Code mode's answer text before it's shown
-        // in the chat bubble — the code itself now lives only in the Canvas panel,
-        // the chat bubble just gets the explanation plus a small chip to open it.
+        // Removes every fenced code block from Code mode's answer text before it's shown in the
+        // chat bubble — the code itself only ever lives in the Canvas panel, the chat bubble just
+        // gets the explanation plus a small chip to open it. Multi-file responses assemble one
+        // fenced block per file (see app.py's final_response for is_multi builds), so this has
+        // to strip ALL of them, not just the first, or every file after the first would still
+        // show up as a raw code block in chat.
         function stripCodeFence(text) {
             if (!text) return text;
-            return text.replace(/```[\w+#.-]*\n[\s\S]*?```/, '').trim();
+            return text.replace(/```[\w+#.-]*\n[\s\S]*?```/g, '').trim();
         }
 
-        // Small "open the artifact" chip, shown in place of the removed code block.
-        // Clicking it (re)opens the Canvas panel — this is the ONLY other way besides
-        // auto-open to bring the panel back after the user has closed it with X.
-        function appendArtifactChip(container, language, code) {
-            if (!code) return;
+        // Small "open the artifact" chip, shown in place of the removed code block(s). Clicking
+        // it (re)opens the Canvas panel — this is the ONLY other way besides auto-open to bring
+        // the panel back after the user has closed it with X. Pass either `files` (a
+        // {filename: code} map, for multi-file builds) or `language`/`code` for a single file.
+        function appendArtifactChip(container, { language, code, files } = {}) {
+            const hasFiles = files && Object.keys(files).length > 0;
+            if (!hasFiles && !code) return;
             const chip = document.createElement('div');
             chip.className = 'artifact-chip';
-            const fileName = `code.${fileExtensionFor(language)}`;
-            const timestamp = new Date().toLocaleString([], { day: 'numeric', month: 'short', hour: '2-digit', minute: '2-digit' });
+            const fileCount = hasFiles ? Object.keys(files).length : 1;
+            const title = hasFiles
+                ? (fileCount === 1 ? Object.keys(files)[0] : `${fileCount} files`)
+                : `code.${fileExtensionFor(language)}`;
+            const meta = hasFiles && fileCount > 1
+                ? `${fileCount} files`
+                : new Date().toLocaleString([], { day: 'numeric', month: 'short', hour: '2-digit', minute: '2-digit' });
             chip.innerHTML = `
                 <div class="artifact-chip-icon">
                     <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 18 22 12 16 6"></polyline><polyline points="8 6 2 12 8 18"></polyline></svg>
                 </div>
                 <div class="artifact-chip-info">
-                    <div class="artifact-chip-title">${fileName}</div>
-                    <div class="artifact-chip-meta">${timestamp}</div>
+                    <div class="artifact-chip-title">${escapeHtml(title)}</div>
+                    <div class="artifact-chip-meta">${escapeHtml(meta)}</div>
                 </div>
                 <button type="button" class="artifact-chip-btn">Open</button>
             `;
@@ -1005,10 +1094,11 @@
         const modes = {
             chat: { sessionId: generateUUID(), html: '', hasMessages: false },
             code: { sessionId: generateUUID(), html: '', hasMessages: false,
-                // Canvas is Code-mode-only state: the last generated file, whether
-                // it's live-previewable, which tab is active, and whether the user
-                // has closed it. Chat mode has no equivalent and never reads this.
-                canvas: { open: false, language: '', code: '', showPreview: false, tab: 'code' }
+                // Canvas is Code-mode-only state: every file the last build produced (keyed
+                // by filename, in build order), which one is active/live, which tab is
+                // showing, and whether the user has closed the panel. Chat mode has no
+                // equivalent and never reads this.
+                canvas: { open: false, files: {}, order: [], activeFile: null, liveFile: null, tab: 'code' }
             }
         };
         let currentMode = 'chat';
@@ -1111,7 +1201,7 @@
             modes[currentMode].hasMessages = false;
 
             if (currentMode === 'code') {
-                modes.code.canvas = { open: false, language: '', code: '', showPreview: false, tab: 'code' };
+                modes.code.canvas = { open: false, files: {}, order: [], activeFile: null, liveFile: null, tab: 'code' };
                 codeCanvas.hidden = true;
             }
 
@@ -1398,8 +1488,7 @@
             let thinkingDone = false;      // true once the model has moved on to answering
             let answerStarted = false;     // true once the first answer token has arrived
             let thinkingFinalized = false; // true once "Thought for Ns" has been set for good
-            let liveThoughtEl = null;      // growing text node for the raw token-by-token reasoning stream (Code Mode)
-            let inlineCodeState = null;    // the in-chat Claude-style code card currently being filled (Code Mode)
+            let inlineCodeStatus = null;   // the "Writing <file>…" status chip currently shown in chat (Code Mode) — never shows code itself
             let lastThoughtStep = null;    // step name of the most recently rendered thought bubble
             let lastThoughtContentEl = null; // its .thought-content node, so a repeat 'thinking'
                                               // heartbeat can update it in place instead of piling
@@ -1487,36 +1576,6 @@
                 scrollToBottom();
             }
 
-            // Genuine token-by-token reasoning stream (Code Mode only, before the plan/
-            // execute loop starts). Creates one growing thought bubble that fills in as
-            // 'thinking' delta events arrive, instead of the discrete per-node bubbles
-            // addThought() creates for 'status' events. Returns the text node to append to.
-            function startLiveThought() {
-                if (thinkingDone) return null;
-                thinkingBlock.style.display = 'block';
-                thinkingToggle.textContent = 'Thinking';
-                thinkingToggle.classList.add('shimmer');
-                thinkingContent.classList.add('expanded');
-
-                const item = document.createElement('div');
-                item.className = 'thought-item';
-                item.innerHTML = `<div class="thought-icon-wrapper">${thoughtIcons['default']}</div>`;
-
-                const content = document.createElement('div');
-                content.className = 'thought-content expanded';
-                item.appendChild(content);
-
-                thinkingContent.appendChild(item);
-                scrollToBottom();
-                return content;
-            }
-
-            function appendLiveThought(delta) {
-                if (!liveThoughtEl || !delta) return;
-                liveThoughtEl.textContent += delta;
-                scrollToBottom();
-            }
-
             // Called the moment the answer actually starts streaming. Switches the main
             // toggle from a shimmering "Thinking" to a shimmering "Answering" and
             // collapses the thought log, but doesn't settle the final label yet.
@@ -1588,14 +1647,7 @@
                             const jsonStr = line.slice(5).trim();
                             try {
                                 const parsed = JSON.parse(jsonStr);
-                                if (parsed.type === 'thinking_start') {
-                                    liveThoughtEl = startLiveThought();
-                                } else if (parsed.type === 'thinking') {
-                                    appendLiveThought(parsed.delta);
-                                } else if (parsed.type === 'thinking_done') {
-                                    // Real per-node status bubbles (understanding/planning/etc.)
-                                    // follow right after this — nothing else to do here.
-                                } else if (parsed.type === 'status') {
+                                if (parsed.type === 'status') {
                                     addThought(parsed.label, parsed.detail, parsed.step);
                                 } else if (parsed.type === 'activity') {
                                     addActivity(parsed);
@@ -1603,16 +1655,16 @@
                                     finalizeActivitySummary(parsed);
                                 } else if (parsed.type === 'code_start') {
                                     startCodeCanvasStream(parsed.language, '');
-                                    inlineCodeState = startInlineCodeStream(textContainer, parsed.language, '');
+                                    inlineCodeStatus = startInlineCodeStatus(textContainer, parsed.language, '');
                                 } else if (parsed.type === 'code_file_start') {
-                                    // Multi-file builds: the previous file's box is done being
-                                    // "live" — remove it before starting the next file's.
-                                    removeInlineCodeStream(inlineCodeState);
+                                    // Multi-file builds: the canvas keeps every earlier file's
+                                    // content (see startCodeCanvasStream) — only the chat status
+                                    // chip's label needs to move onto the new filename.
                                     startCodeCanvasStream(parsed.language, parsed.filename);
-                                    inlineCodeState = startInlineCodeStream(textContainer, parsed.language, parsed.filename);
+                                    if (!inlineCodeStatus) inlineCodeStatus = startInlineCodeStatus(textContainer, parsed.language, parsed.filename);
+                                    else updateInlineCodeStatus(inlineCodeStatus, parsed.language, parsed.filename);
                                 } else if (parsed.type === 'code_delta') {
-                                    appendCodeCanvasStream(parsed.delta);
-                                    appendInlineCodeStream(inlineCodeState, parsed.delta);
+                                    appendCodeCanvasStream(parsed.delta, parsed.filename);
                                 } else if (parsed.type === 'code_result') {
                                     codeResult = parsed;
                                 } else if (parsed.type === 'message_reset') {
@@ -1647,16 +1699,17 @@
                         }
                     }
 
-                    if (mode === 'code' && codeResult && codeResult.code) {
+                    const hasCodeResult = codeResult && (codeResult.code || (codeResult.files && Object.keys(codeResult.files).length > 0));
+                    if (mode === 'code' && hasCodeResult) {
                         setProse(textContainer, marked.parse(normalizeMathDelimiters(stripCodeFence(assistantFullText))));
                         highlightCode(textContainer);
-                        // Generation is done — the live box's job is over. The finished file
-                        // goes to the canvas + chip, same as before this box existed.
-                        removeInlineCodeStream(inlineCodeState);
-                        appendArtifactChip(textContainer, codeResult.language, codeResult.code);
-                        updateCodeCanvas(codeResult.language, codeResult.code, codeResult.show_preview);
+                        // Generation is done — the live status chip's job is over. The finished
+                        // file(s) go to the canvas + artifact chip instead.
+                        removeInlineCodeStatus(inlineCodeStatus);
+                        appendArtifactChip(textContainer, { language: codeResult.language, code: codeResult.code, files: codeResult.files });
+                        updateCodeCanvas({ language: codeResult.language, code: codeResult.code, files: codeResult.files, fileLanguages: codeResult.file_languages, showPreview: codeResult.show_preview });
                     } else {
-                        removeInlineCodeStream(inlineCodeState);
+                        removeInlineCodeStatus(inlineCodeStatus);
                         setProse(textContainer, marked.parse(normalizeMathDelimiters(assistantFullText)));
                         highlightCode(textContainer);
                     }
@@ -1664,13 +1717,14 @@
                 } else {
                     const data = await response.json();
                     assistantFullText = data.response;
-                    if (mode === 'code' && data.code) {
+                    const hasCode = data && (data.code || (data.files && Object.keys(data.files).length > 0));
+                    if (mode === 'code' && hasCode) {
                         setProse(textContainer, marked.parse(normalizeMathDelimiters(stripCodeFence(assistantFullText))));
                         highlightCode(textContainer);
                         // Non-streaming response: the full code arrives in one shot, so there's
                         // no "live" phase to show — go straight to the chip, as before.
-                        appendArtifactChip(textContainer, data.language, data.code);
-                        updateCodeCanvas(data.language, data.code, data.show_preview);
+                        appendArtifactChip(textContainer, { language: data.language, code: data.code, files: data.files });
+                        updateCodeCanvas({ language: data.language, code: data.code, files: data.files, fileLanguages: data.file_languages, showPreview: data.show_preview });
                     } else {
                         setProse(textContainer, marked.parse(normalizeMathDelimiters(assistantFullText)));
                         highlightCode(textContainer);
